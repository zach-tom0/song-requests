import classes from "./NowPlaying.module.css";

const NowPlaying = () => {
  return (
    <div className={classes.nowplaying}>
      <h2>Now Playing</h2>
    </div>
  );
};

export default NowPlaying;
